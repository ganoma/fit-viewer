# fit-viewer — トライアスロン練習ログ可視化

Garmin の .fit ファイルを取り込み、ラン／バイク／スイムの指標、閾値推定、練習日記を扱う個人用ツール。
利用者は作者ひとり。

## 構成

| パス | 内容 |
|---|---|
| `src/` | React 19 + TypeScript。ビルドは Vite 8。グラフは plotly.js |
| `server/` | Express 5 の .mjs。`index.mjs`（ルーティング）、`summary.mjs`、`curves.mjs`、`thresholds.mjs`、`auth.mjs` |
| `data/` | 永続データ。アクティビティごとに `<id>.fit`（原本）・`<id>.json`（サマリ）・`<id>.note.md`（日記）をファイルで置き、付随メタデータは SQLite（`fitviewer.db`、`node:sqlite`）。`DATA_DIR` で変更可。git 管理外 |
| `docs/` | 役割間の引き継ぎ文書（`requirements/` → `design/`）。詳細は `docs/README.md` |

親ディレクトリ `../` には前身の Python 分析スクリプトと別系統の実験（`../stock-predictor/`）があるが、
このアプリとは無関係。混同しないこと。

## 検証コマンド

```
npm run lint     # oxlint
npm run build    # tsc -b && vite build（型チェック兼用）
```

テストフレームワークは未導入。検証は型チェック・ビルド・実画面の確認で行う。
dev サーバーは `.claude/launch.json` の `fit-viewer`（5173）、API サーバーは `server/index.mjs`。

## 共通ルール

- **コード内のコメント・ドキュメント・コミットメッセージは日本語で書く**
- 既存ファイルの命名・書き方・データフローに合わせる。個人の流儀を持ち込まない
- 汎用化・多機能化より、作者が実際に使う導線を優先する
- 汎用機能に育ちそうなものは、このアプリに抱き込まず別アプリとして切り出す
- 作業は `feature/<機能名>` ブランチで行い、main へ直接コミットしない
- GitHub 操作は git コマンドで行う。gh CLI は導入しない

## 役割分担

`.claude/agents/` に3役を定義してある。

| 役 | 権限 | 仕事 |
|---|---|---|
| `pm` | docs のみ書込 | 要望 → 要件定義。実装後の受け入れ判定 |
| `architect` | docs のみ書込 | 要件 → 設計書。既存コードを読んで方針を決める |
| `implementer` | 全ツール | 設計書 → コード。lint・ビルド・動作確認まで |

**このメインセッションがオーケストレーターを兼ねる。** 役を指名して振り、成果物を確認し、次の役へ渡す。
規模の小さい変更まで3役を通す必要はない。要件が曖昧なとき、設計判断が必要なとき、
あるいは長い作業でコンテキストを分けたいときに使う。

引き継ぎは必ず `docs/` のファイル経由で行うこと。サブエージェントは互いの会話を見ていない。
